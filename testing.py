from flask import Flask
flask = (FastApi)

from django.contrib.auth.models import User

user = User.objects.create()

print(user)

from django.db import *
class User:
    name = "Enter the Name here: " 
    last_name = "Enter the last name here: "

    print(name, last_name)